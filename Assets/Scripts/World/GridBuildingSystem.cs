using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridBuildingSystem : MonoBehaviour
{
    //todo can we make a building layer in local map and use that instead of xy grid?
    // might not need to since we can grab entities of a certain type from the local map
    // maybe just use Grid Object
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
